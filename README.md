# critical-reader

An app for making reading more productive

## Set up

Create .env file with relevant environment variables.

Create Django admin super user.

Create configuration.

Run TUI. Create project. Assign config to it. Add resources to it.

Start job runner.
